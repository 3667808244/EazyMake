# EazyMake 1.2.0-dev.3 — 默认项目模板内建 Debug/Release Profile

> 本计划并入 1.2.0 正式发布版。发布总览见 [`1.2.0.md`](1.2.0.md)。

---

## 1 背景

`ezmk project new` 通过 `config::write_default_config()`（`src/config.cpp`）生成的默认 `ezmk.toml` 目前只有基准 `[compile].flags = ["-Wall", "-Wextra", "-O2"]`，没有内建任何构建 profile。用户想获得 Debug / Release 两种构建形态，需要自行查阅 `docs/config_file.md` 手写 `[compile.profile.*]` 段——对新手不友好，也容易写错（如漏掉 `msvc_flags`）。

`[compile.profile.<name>]` 机制（0.2.3+）与文档中的 Debug/Release 示例早已存在，缺的只是**把它固化进默认模板**：让新项目开箱即有 Debug / Release 两个 profile，`ezmk build --profile debug|release` 直接可用。

同时修正一个与 profile 体系相悖的现状：当前基准 `-O2` 隐含"默认即优化"，但有了显式 profile 后，"优化级别属于 profile"才是清晰语义——基准应只保留通用警告标志。

补充一个**默认 Profile 配置项**：新增 `[compile].default_profile`，声明"未显式 `--profile` 时默认使用哪个 profile"。模板内建 `default_profile = "debug"`——开箱 `ezmk build` 默认构建即可调试（`-g -O0`、断言开启），需要优化时显式 `--profile release`。

---

## 2 目标

| # | 目标 | 优先级 |
|---|------|--------|
| 1 | 默认 `ezmk.toml` 模板内建 `[compile.profile.debug]` 与 `[compile.profile.release]`（含 `flags` + `msvc_flags`），跨 GCC/Clang/MSVC 一致 | P0 |
| 2 | 基准 `[compile].flags` 收敛为警告-only（`["-Wall", "-Wextra"]`），优化级别移入 profile | P0 |
| 3 | 新增 `[compile].default_profile` 配置项：声明"无显式 `--profile` 时默认使用的 profile"；模板内建 `"debug"` | P0 |
| 4 | 新项目开箱 `ezmk build` 默认即 debug 构建；`--profile debug` / `--profile release` 显式可用 | P0 |
| 5 | 仅影响**新创建**项目；既有 `ezmk.toml` 不变 | P0 |
| 6 | 单测 + 集成测试覆盖模板内容、default_profile 解析/回退与 profile 构建 | P0 |
| 7 | 文档同步（config_file.md 改写 "not auto-apply" 表述 / project new 说明） | P1 |

---

## 3 详细设计

### 3.1 目标模板（`write_default_config()` 变更）

`src/config.cpp:739` 的 `write_default_config()` 生成内容变更为：

```toml
[project]
name = "<name>"
type = "<type>"
version = "0.1.0"
language = "C++17"

[compile]
flags = ["-Wall", "-Wextra"]
default_profile = "debug"
include_dirs = ["include"]

[compile.profile.debug]
flags = ["-g", "-O0"]
msvc_flags = ["/Zi", "/Od"]

[compile.profile.release]
flags = ["-O2", "-DNDEBUG"]
msvc_flags = ["/O2", "/DNDEBUG"]

[link]
flags = []
link_dirs = []
system_target = []

[depends]
lib = []
```

`type = "utils"` 时仍追加 `[utils] tools = []`（与现状一致）。

**要点**：

- **基准只留警告**：移除 `-O2`。优化级别由 profile 显式决定。
- **默认构建 = debug**：`default_profile = "debug"` 使无 `--profile` 的默认构建 = `-Wall -Wextra -g -O0`（可调试、断言开启、无优化）；需要优化时显式 `--profile release`。
- **debug**：GCC/Clang `-g -O0`（可调试、不优化），MSVC `/Zi /Od`。
- **release**：GCC/Clang `-O2 -DNDEBUG`（优化 + 关闭断言），MSVC `/O2 /DNDEBUG`。
- **不内建 `[link.profile.*]`**：链接 profile 为空 `flags = []` 时与缺省等价，模板不加无意义空段；用户有链接专属 profile 标志（如 `-flto`）时自行追加。
- **不内建 profile macros**：如 debug 注入 `DEBUG=1` 等为可选增强，用户按需在 `[compile.profile.<name>.macros]` 追加，模板保持最小。

### 3.2 profile 语义对齐

- **default_profile 解析**：新增 `[compile].default_profile`（`CompileSection` 字符串字段，可空）。生效顺序：**显式 `--profile <name>` > `[compile].default_profile`（非空时）> 基准-only**。
  - 无 `--profile` 且 `default_profile` 非空 → 按该名字执行一次 profile 合并（与显式 `--profile` 走同一 lookup/合并/报错路径）。
  - `default_profile` 指向不存在的 profile → 复用现有 unknown-profile 报错（含 did-you-mean 建议）。
  - 旧配置无此字段（或为空）→ 无 `--profile` 时仍基准-only，行为不变。
- 合并规则不变（`docs/config_file.md`）：profile `flags`/`msvc_flags` **追加**到 base 之后。
- 因基准已无 `-O*`，profile 与 base 之间不存在 `-O2`/`-O0` 先后覆盖歧义：
  - 默认（模板 `default_profile = "debug"`）→ `-Wall -Wextra -g -O0`
  - `--profile debug` → `-Wall -Wextra -g -O0`（与默认一致）
  - `--profile release` → `-Wall -Wextra -O2 -DNDEBUG`（显式覆盖 default_profile）
- `--profile` 同时作用于 compile 与 link；无 link profile 时链接沿用基准（空）。
- **消费点一致性**：`ezmk build` / `ezmk project watch` / `ezmk project cc` / `ezmk project export cmake` 四处无 `--profile` 时统一回退 `default_profile`，保证 compile_commands.json 与 CMake 导出和默认构建一致。

### 3.3 行为变更说明（需在 CHANGES.md 明确）

| 变更 | 影响范围 | 说明 |
|------|----------|------|
| 基准 `-O2` 移除 | **仅新建项目** | 旧项目 `ezmk.toml` 不被改写，行为不变 |
| 新增 `[compile].default_profile`（模板内建 `"debug"`） | 新建项目 | 无 `--profile` 的默认构建 = debug（`-g -O0`、断言开启、无优化）；需优化时显式 `--profile release`——"优化属于 profile"的明确语义 |
| 新增 debug/release profile | 新建项目 | 开箱可用 `--profile debug` / `--profile release`；默认构建即 debug |

### 3.4 涉及文件

| 文件 | 变更 |
|------|------|
| `include/ezmk/config.hpp` | `CompileSection` 增 `std::string default_profile;`（config.hpp:39-49） |
| `src/config.cpp` | `parse_compile()`（385-473）读取 `[compile].default_profile`；`write_default_config()` 模板更新（base 去 `-O2` + 两个 profile 段 + `default_profile = "debug"`） |
| `src/build.cpp` | `prepare_build_state()`（494-533）`opts.profile` 为空时回退 `cfg.compile.default_profile`，复用 profile 合并/unknown 报错路径 |
| `src/main.cpp` | `project watch`（240-249）与 `project cc`（201）无 `--profile` 时同款回退 |
| `src/export.cpp` | `export cmake`（133-134）无 `--profile` 时回退 `default_profile` |
| `test/test_config.cpp` | 单测：`write_default_config()` 输出含 `[compile.profile.debug]` / `[compile.profile.release]` / `default_profile = "debug"` 及期望 flags/msvc_flags；base 不含 `-O2`；parse `[compile].default_profile`（合法名/缺省为空）；`parse_config()` round-trip；既有 `write_default_config: no profile or hooks sections`（:1459）断言更新 |
| `test/test_integration.cpp` | 集成：`ezmk project new` → 校验生成 `ezmk.toml` 含 profile 段 + `default_profile = "debug"`；无 `--profile` `ezmk build` 默认 debug flags（`-g -O0`）；`--profile debug` / `--profile release` 成功 |
| `docs/en/config_file.md` / `docs/zh/config_file.md` | `[compile]` 段补 `default_profile` 字段；改写 "Profiles do not auto-apply" → default_profile 例外 + 优先级；模板内建说明 |
| `docs/en/cli.md` / `docs/zh/cli.md` | `project new` 说明提及新模板含 profile + `default_profile = "debug"` |
| `CHANGES.md` | dev.3 条目 + 行为变更说明 |

---

## 4 执行步骤

- [ ] **4.1 模板**：`write_default_config()` 更新 base（去 `-O2`）、profile 段与 `default_profile = "debug"`
- [ ] **4.2 配置项**：`CompileSection.default_profile` 字段 + `parse_compile()` 读取 `[compile].default_profile`
- [ ] **4.3 回退逻辑**：`prepare_build_state()` + `project watch` + `project cc` + `export cmake` 无 `--profile` 时回退 `default_profile`
- [ ] **4.4 单测**：`test_config.cpp` 模板内容断言 + default_profile 解析 + `parse_config()` round-trip（含既有用例更新）
- [ ] **4.5 集成**：`ezmk project new` → 校验模板；无 `--profile` 默认 debug 构建；`--profile debug/release` 构建
- [ ] **4.6 文档**：config_file.md（改写 auto-apply 表述）/ cli.md（en+zh）+ CHANGES.md
- [ ] **4.7 回归**：全量测试零回归（基线 546/2617）

---

## 5 兼容性

| 变更 | 影响 | 处理 |
|------|------|------|
| 新增 profile 段（模板） | 纯新增 | 新项目开箱可用；旧项目不变 |
| 新增 `[compile].default_profile`（可选字段） | 纯新增 | 旧配置无此字段 → 无 `--profile` 时仍基准-only，行为不变；模板内建 `"debug"` 仅影响新建项目 |
| base 移除 `-O2`（模板） | 新建项目默认构建 = debug（`-g -O0`，无优化、断言开启） | 需要优化时显式 `--profile release`；CHANGES.md 明确 |
| 无新 i18n key | 无 | 模板内容非用户消息，无需 i18n |
| 无新命令 / 无既有字段语义变化 | 无 | 仅新增可选字段；`--profile` 显式优先级不变 |

---

## 6 跨版本关注点

- **与 dev.2（CMake 导出）协同**：默认模板内建 profile 后，`ezmk project export cmake --profile release` 能直接导出与 ezmk 构建一致的优化配置；dev.2 的 profile 映射（P2）受益于新模板自带 `[compile.profile.*]`。
- **与 dev.1（compile_commands）协同**：`ezmk project cc --profile release` 生成的索引命令与 `ezmk build --profile release` 一致——模板自带 profile 使这一对照开箱即可验证。
- **默认构建一致性**：`default_profile` 回退在 build / watch / `project cc` / `export cmake` 四处统一生效——无 `--profile` 时 compile_commands.json 与 CMake 导出和 `ezmk build` 默认构建（debug）一致，避免"构建用 debug、索引/导出用基准"的分叉。
- **dogfooding**：本次改动后，新项目模板即 EazyMake 自身推荐的工程化默认；文档示例与模板保持同源风格。
- **基准语义收敛**：`-O2` 从基准移除是一次性语义收敛（优化归 profile），后续新增优化类基准标志时应避免回填。
