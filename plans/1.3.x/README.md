# 1.3.x — 功能版本系列（Workspace 工作区）

> 1.3.x 系列 = **1.3.0 功能版本**（dev.1 ~ dev.5 + pre.1，规划中）+ 后续补丁（如有）。
>
> 本目录为 1.3.x 全部文档的**平铺结构**（无子文件夹）：`1.3.0-dev.N.md` 为开发子版本，`1.3.0-pre.N.md` 为发布前收口，`1.3.0.md` 为正式版聚合。

## 定位

1.3.0 是 1.2.x 收官后的**首个功能 minor**（延续 1.0.0 → 1.1.0 → 1.2.0 的节奏）：新增 **Workspace（工作区）** 功能——一个目录下若干独立项目（成员）的集合，提供统一、可并行的批量管理（build / test / clean）。成员间可声明**单向非循环依赖**（最常见 monorepo 形态：共享基础库 + 多个可执行文件），构建时**拓扑排序 + 产物复用**（改库 `.cpp` → 消费者只重链接，消灭重复编译）。**纯增量：新命令组 + 新配置文件 `ezmk-workspace.toml` + `[depends] workspace` 新字段，公共 API 零破坏**（破坏性变更仍仅归 2.0.0）。同系列附带 **i18n 语言变体**（dev.4，`zh-TW` 繁体等，继承式变体文件）与**消费命令总是自动构建**（dev.5，`test`/`pack --precompiled` 消除陈旧产物陷阱）。

## 版本规划（dev / pre 拆分）

> 1.3.0 正式版按 dev → pre 两个阶段推进：dev 落地功能，pre 做文档/门槛收口，最后聚合发布。

| 子版本 | 主题 | 关键交付 | 状态 |
|--------|------|----------|------|
| [1.3.0-dev.1](1.3.0-dev.1.md) | workspace 配置、定位与依赖校验 | `ezmk-workspace.toml` 解析（`[workspace]` + `[workspace.options]`）+ `locate_workspace_root()` + 成员校验（路径逃逸 / 存在性 / 嵌套）+ **`[depends] workspace` 解析 + 环检测 + 被依赖类型约束** + 单测 | ✅ 已完成（2026-08-19，dev.1） |
| [1.3.0-dev.2](1.3.0-dev.2.md) | `ezmk workspace` 命令组与拓扑构建 | `list` / `build` / `test` / `clean` / `--member`（含依赖闭包）/ `--stop-on-error` + **Kahn 拓扑分层 + 层内并行** + **兄弟产物注入（成员自发现，零环境变量）** + **响应文件兜底** + 跨成员增量 + `-w` 重定向 + i18n key | ✅ 已完成（2026-08-21，dev.2） |
| [1.3.0-dev.3](1.3.0-dev.3.md) | 测试与 CI | 集成测试（依赖构建顺序 / 跨成员增量 / 并行 / 失败汇总 / 校验拒绝）+ CI 步骤 + 全量零回归 | 规划中 |
| [1.3.0-dev.4](1.3.0-dev.4.md) | **i18n 语言变体** | **BCP 47 变体标签解析**（`zh_CN`/`zh-CN.UTF-8` → `zh-CN`）+ **继承式变体文件**（`locale/zh-TW.json` 只写差异键，缺键回退基础语言）+ **回退链**（变体 → 基础 → 英文）+ `check_i18n.py` 变体校验 + 单测 | 规划中 |
| [1.3.0-dev.5](1.3.0-dev.5.md) | **消费命令总是自动构建** | `ezmk test` / `ezmk project pack --precompiled` 移除存在性门控，**一律先增量构建再消费**（消除「改源码后旧产物仍在 → 跑旧代码/打旧包」的陈旧产物陷阱）；utils 例外保留 + 回归用例锁定 | 规划中 |
| [1.3.0-pre.1](1.3.0-pre.1.md) | 文档与发布收口 | cli.md / README / 教程 / CHANGES.md + **non-goals「多项目工作区」条款更新** + 发布门槛复核（API 无破坏性变更 + 全量零回归） | 规划中 |
| [1.3.0](1.3.0.md) | 正式发布 | 聚合 dev.1 ~ dev.5 + pre.1；发布门槛 + 分发（winget/Homebrew/pacman） | 规划中 |

### 依赖关系

```
1.2.4 (已发布) ──→ 1.3.0-dev.1 (配置/定位/依赖校验) ──→ 1.3.0-dev.2 (命令/拓扑/注入)
                      ├──────────────────┬──────────→ 1.3.0-dev.3 (测试/CI)
                      │                  ├──────────→ 1.3.0-dev.4 (i18n 语言变体)
                      │                  └──────────→ 1.3.0-dev.5 (消费命令总是构建) ──→ 1.3.0-pre.1 (文档收口) ──→ 1.3.0 (发布)
                      └──────────────────────────────────────────────↗
```

- dev.1 → dev.2 顺序依赖（命令消费配置解析、定位与依赖校验结果）。
- dev.2 → dev.3 顺序依赖（集成测试覆盖命令与拓扑行为）。
- **dev.4 / dev.5 与 workspace 主线并行**：dev.4（i18n 模块独立，dev.1 后即可开始）；dev.5（`run_tests`/`pack_project` 单项目路径，dev.1 后即可开始；其语义自动惠及 dev.2/dev.3 的 `workspace test` 成员透传）。
- pre.1 依赖全部 dev（含 dev.4/dev.5）完成。
- 与 **2.0.0 解耦**：workspace 纯增量，不依赖任何 deprecation 到期；2.0.0 保持破坏性变更窗口定位。

## 跨版本关注点

- **纯增量兼容**：新命令组 + 新配置文件 + `[depends] workspace` 新字段，不触碰单项目路径（`project`/`pkg`/`repo`/`example` 全部不变）；既有 `ezmk.toml` 零影响。
- **回归基线**：当前全量测试 822 用例 / 3922 断言（1.3.0-dev.1 后），新增功能不得引入回归。
- **并行模型**：跨成员并行按**拓扑层**（依赖层先构建，同层并行，复用线程池思路）；成员内 `-j` 并行不变；输出加成员前缀防交错；`--stop-on-error` = 失败后停派发（后续层 skipped）、不 kill 在跑成员。
- **注入与命令行长度**：兄弟产物注入走**成员自发现**（零环境变量，长度与规模无关）；编译/链接命令 >16K 走 GCC 响应文件兜底——大型项目不炸（坑 1）。
- **依赖约束**：成员依赖仅允许**单向非循环**——环（含自环）与「被依赖成员非 static」在配置校验期 fail-fast；完整构建图（版本 / 平台矩阵 / 可编程图）仍归 non-goals。
- **安全**：成员路径逃逸（`../` / 绝对 / 盘符 / 符号链接出根）在配置校验期拒绝。
- **i18n**：新增 `workspace_*` key（X-macro 三向一致，`check_i18n.py` 通过）；**语言变体**（dev.4）——BCP 47 标签归一化 + 继承式变体文件 + 回退链（变体 → 基础 → 英文），新键在变体文件未转写时自动回退，永不出现 `{???}`。
- **消费命令一致性**（dev.5）：所有消费构建产物的命令统一「先增量构建再消费」——`run`/`install`/`watch`/`test`/`pack --precompiled`；`pack` 源码包与 `cc`/`export`/`import`/`clean` 不消费产物，维持现状。增量缓存保证新鲜产物时开销趋近于零。
