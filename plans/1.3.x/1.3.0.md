# EazyMake 1.3.0 — Workspace（工作区）功能版本

> 1.2.x 收官后的**首个功能 minor**。发布总览见 [`1.3.x/README.md`](README.md)。
> 正式版按 **dev → pre** 两阶段推进：dev 落地功能，pre 做文档/门槛收口，最后聚合发布。

---

## 1 背景

EazyMake 目前只有**单项目模型**：`ezmk build` 从当前目录向上找 `ezmk.toml`，一次处理一个项目。多个独立项目（monorepo 多产物、一批示例/工具、CI 多组件）只能逐个 `cd` 或外部脚本循环，没有统一管理面。`@link:` 是源码级共享（跨目录编译进多个项目），但每次改共享源码，所有消费者各自重编一份——重复劳动。

需要一个**多项目组织层**：工作区 = 一个目录下多个独立项目（成员）的集合，统一、可并行的批量管理。成员间可声明**单向非循环依赖**（最常见 monorepo 形态：共享基础库 + 多个可执行文件），构建时**拓扑排序 + 产物复用**：改库 `.cpp` → 库重编一次、消费者只重链接（消灭 @link 的重复编译）；改库 `.h` → 消费者重编（C++ 编译模型固有，任何工具无法消除）。

**定位**：这是「批量命令 + 并行调度 + 最小依赖图（单向、无环、无版本）」，**不是**完整构建系统——环 / 版本约束 / 平台矩阵 / 可编程构建图仍归 non-goals（见 [`docs/non-goals.md`](../../docs/zh/non-goals.md)「多项目工作区」条款的更新）。

## 2 目标

| # | 目标 | 优先级 |
|---|------|--------|
| 1 | 独立配置文件 `ezmk-workspace.toml`（`[workspace] members` + `[workspace.options]`）+ `locate_workspace_root()` 定位 | P0 |
| 2 | 成员依赖声明：成员 `ezmk.toml` 的 `[depends] workspace = ["<成员名>"]`（命名空间区别于外部包） | P0 |
| 3 | 依赖约束：**单向非循环**（环在配置校验期拒绝）+ 被依赖成员为 `static`（产物 `build/lib<name>.a`）；无版本（开发中即改即用） | P0 |
| 4 | 顶层 `ezmk workspace` 命令组：`list` / `build` / `test` / `clean` / `--member`（含依赖闭包）/ `--stop-on-error`；`build/test/clean` 附 `-w` = 重定向到 workspace 命令组 | P0 |
| 5 | 构建语义：Kahn 拓扑分层（依赖层先构建）+ 层内并行 + 兄弟产物自动注入（**成员自发现**，`-I include` + `-L build -l<name>`，零环境变量） | P0 |
| 6 | 增量：改库 `.cpp` → 库重编 + 消费者仅重链接；改库 `.h` → 消费者经 depfile 自动重编 | P0 |
| 7 | 安全边界：成员路径逃逸拒绝 + 环拒绝 + 依赖成员类型校验 | P0 |
| 8 | **命令行长度兜底**：编译/链接命令超阈值 → GCC 响应文件（大型项目不炸的通用加固） | P1 |
| 9 | 测试：单元（解析/拓扑/环检测/校验/注入解析/响应文件）+ 集成（拓扑构建/增量/失败汇总/拒绝分支）；全量零回归 | P0 |
| 10 | 文档：cli.md / README / 教程 / CHANGES.md 1.3.0 条目；non-goals 条款同步更新 | P1 |
| 11 | 公共 API 无破坏性变更（新命令组 + 新配置文件 + `[depends] workspace` 新字段；单项目路径零改动） | P0 |
| 12 | **i18n 语言变体**（dev.4）：BCP 47 变体标签（`zh-TW` 等）+ 继承式变体文件 + 回退链 | P1 |

## 3 详细设计

### 3.1 配置文件 `ezmk-workspace.toml`（独立于 `ezmk.toml`）

```toml
[workspace]
name = "my-workspace"
members = ["libs/strutil", "apps/tool-a", "apps/tool-b"]

[workspace.options]
default_jobs = 0          # 0 = 自动（hardware_concurrency）；默认
stop_on_error = false     # 默认继续并汇总；true = 首个失败即停
```

- 文件放 **workspace 根**；成员路径**相对根**，仅允许子目录（`../` 逃逸 → 校验错误）
- 根目录可有自己的 `ezmk.toml`（根自身可作为成员）或仅作纯容器
- 新解析器（复用 toml11）；`[workspace]` 的 `name` 可选、`members` 必填非空；`[workspace.options]` 可选
- **定位**：`locate_workspace_root()` 从 cwd 向上最多 5 层找 `ezmk-workspace.toml`（与 `locate_project_root` 对称，互不干扰）

### 3.2 成员依赖声明（成员自己的 `ezmk.toml`）

```toml
# apps/tool-a/ezmk.toml
[depends]
lib = ["fmt"]                     # 外部包（现状，不变）
workspace = ["strutil"]           # 1.3.0：兄弟成员（命名空间区别于外部包）
```

- 依赖是**项目的属性**，跟随成员走：成员搬到别处/独立出去，声明不用改
- `workspace` 值 = 成员相对路径的**末段**（`strutil`）或完整相对路径（`libs/strutil`），二者皆可
- **约束**：
  - **单向无环**：A→B 且 B→A 或自环 → 配置校验期报错（fail-fast，同路径逃逸/依赖互引哲学）
  - **被依赖成员必须 `static`**（产物 `build/lib<name>.a`）；`executable`/`shared` 不能被依赖（shared 运行时依赖归延后）
  - 依赖方可为 `executable`/`static`（`shared` 依赖兄弟成员暂不支持，延后）
  - **无版本**：开发中源目录即改即用；版本/快照语义继续走包模型（与 1.2.4 目录包互补）

### 3.3 命令面：顶层 `ezmk workspace` 组

```
ezmk workspace list                        # 成员列表：路径 + 类型 + 依赖 + 状态
ezmk workspace build  [build-opts] [-j N] [--stop-on-error] [--member <name>...]
ezmk workspace test   [test-opts]  [-j N] [--stop-on-error] [--member <name>...]
ezmk workspace clean                       # 批量清理（全部成员）
ezmk build -w / ezmk test -w / ezmk clean -w   # -w = 重定向到对应 workspace 命令组
```

- `build-opts` 完全复用单项目 build 选项（`--profile` / `--disable-cache` / `-j` 等），逐成员透传
- `--member <name>`：只对指定成员执行（**含其依赖闭包**：依赖成员按拓扑先构建，保证产物新鲜）；名称 = 成员路径末段或完整相对路径
  - 只想构建**单个成员、不构建闭包**（接受兄弟产物可能过期）→ `cd <member> && ezmk build`（成员内本地构建，仅注入已存在的兄弟产物）
- **`-w` / `--workspace`**（附在 `build/test/clean` 上）：把命令**重定向**到 workspace 命令组——`ezmk build -w` ≡ `ezmk workspace build`（自动向上定位根，根内任意子目录可用）。**不是**「项目 + workspace 都构建」；不加 `-w` 的成员内 `ezmk build` 仍是单项目语义（只注入已存在的兄弟产物）
- 顶层 `ezmk build` 在纯容器根 → 提示用 `ezmk workspace build`（或 `ezmk build -w`）；有 `ezmk.toml` → 正常单项目行为
- 无别名；未知子命令/未知成员 → fatal
- **i18n**：新增 `workspace_*` key 组（X-macro 三向一致）

### 3.4 构建语义：拓扑 + 层内并行

```
ezmk workspace build
[workspace] Building 3 members (-j 2)...
  ✓ libs/strutil     (2.1s)   ← 第 0 层（无依赖）
  ✓ apps/tool-a      (0.8s)   ← 第 1 层（依赖 strutil），与 tool-b 并行
  ✓ apps/tool-b      (0.7s)
[workspace] 3 succeeded, 0 failed
```

- **Kahn 拓扑分层**：依赖成员先构建；同层成员并行（`-j` 限并发，复用 `util::ThreadPool`）
- 无依赖成员 = 第 0 层（平行批量退化为特例，`1.3.0-dev.2` 的并行调度不变）
- 环在**配置期**拒绝（`workspace list`/任何执行前 fail-fast），构建期不可能出现环
- 执行模型为**子进程**（每成员独立 `<ezmk> build/test/clean` 进程，cwd 为成员目录）——cwd/Lua 状态/缓存天然隔离，复用全部单项目逻辑
- **`--stop-on-error` 精确语义**（`build`/`test` 一致）：首个失败后**停止派发新任务**（本层未启动成员 + 后续层全 `skipped`）；在跑成员**自然结束、不 kill**（避免不确定中间状态）；摘要含 succeeded/failed/skipped，退出码非零。默认（不设 flag）：全部跑完再汇总，任一失败 → 退出码非零。`clean` 不支持该 flag

### 3.5 兄弟产物注入：成员自发现（无环境变量）

当成员声明 `workspace = ["strutil"]` 时，**该成员自己的构建进程**自动注入：

```
-I <workspace>/libs/strutil/include
-L <workspace>/libs/strutil/build  -lstrutil
```

- **机制 = 成员自发现**：成员进程读自身 `[depends] workspace`（非空才进入）→ `locate_workspace_root()` + `load_from(root)`（复用 dev.1）→ 在 members 中解析依赖名 → 追加注入参数（存在性门控：`include/` 存在才 `-I`；`build/lib<name>.a` 存在才 `-L/-l`）
- **零环境变量**：注入信息长度与 workspace 规模无关——不重蹈 `EZK_WS_DEPS=...` 随成员数线性增长、在大型项目撞 Windows 环境块 32KB 上限的坑（见 3.8 坑 1）
- **约定布局**：被依赖成员固定产出 `include/`（公共头）与 `build/lib<name>.a`（静态库）；消费者 `#include <strutil/strutil.hpp>` 开箱即用，零手写路径
- 注入的 `-I` 优先级：成员自身 `include_dirs` > 注入 `-I` > 系统路径
- 产物注入在构建前**解析**（依赖成员必须先构建完成；其 `build/lib<name>.a` 存在才注入链接）——由 workspace 构建的拓扑保证；成员内独立 `ezmk build` 时若兄弟产物缺失 → 提示「先 `ezmk workspace build` 或构建依赖成员」

### 3.6 命令行长度兜底（响应文件）

- 编译/链接命令以整体字符串执行，Windows 侧受 CreateProcess **32767 字符**限制——超长直接失败且报错晦涩（"command line too long"或静默截断）
- 命令长度 > **16K 字符** → 改写为 GCC 响应文件 `@<tmp>.rsp`（参数逐行写入；g++ 原生支持；编译与链接命令都适用）
- 属**通用加固**（深路径大项目同样受益）；注入 `-L/-l` 只是让问题更容易暴露（见 3.9 坑 1）

### 3.7 增量语义

- **改库 `.cpp`** → 库重编一次（`build/lib<name>.a` 更新）→ 消费者 `.o` 缓存命中、**仅重链接**（链接无缓存，天然正确）
- **改库 `.h`** → 消费者编译时经 `-MMD` depfile 记录兄弟头文件 → 缓存自动失效重编（复用 `cache.cpp` 的 `parse_depfile_and_hash`，零新语义）
- 每个成员独立 `.ezmk/`、`build/`、`ezmk.lock`、测试缓存（含已修复的签名校验）——增量天然按成员隔离

### 3.8 成员校验（`list` 与任何执行前）

1. 路径在根内（无 `..` 逃逸、无绝对路径、无盘符、无 UNC；符号链接解析真实路径）
2. 成员目录存在且含 `ezmk.toml`（缺失 → 标记 `invalid` 并跳过，不阻断其他成员）
3. 无嵌套 workspace（成员内不得再有 `ezmk-workspace.toml`）
4. **成员依赖**：`[depends] workspace` 引用的名字必须解析到已列成员（未知 → 报错）；环（含自环）→ 报错；被依赖成员类型非 `static` → 报错

### 3.9 已知坑位与对策（决策记录）

> 设计评审中识别的坑位。对策已落入 dev 各版本；dev.3 用测试锁定。

| # | 坑 | 优先级 | 对策 | 落点 |
|---|-----|--------|------|------|
| 1 | **注入信息经环境变量传递**（`EZK_WS_DEPS=...`）随 workspace 规模线性增长，大型项目撞 Windows 环境块 32KB 上限；注入 `-I/-L/-l` 堆进 g++ 命令行撞 CreateProcess 32767 字符上限——两者都**无声炸** | 高 | **成员自发现**（零环境变量，长度与规模无关）+ **响应文件兜底**（>16K → `@<rsp>`） | dev.2 §3.4/§3.5 |
| 2 | **`--member` 闭包语义**：用户可能以为只构建目标成员 | 中 | 文档明确：`--member X` = X + 依赖闭包；只构建单成员 → `cd <member> && ezmk build`（注入已存在产物，不触发闭包构建） | dev.2 §3.1、cli.md |
| 3 | **`--stop-on-error` 在层内并行下语义模糊**（kill 在跑成员？跳过后续层？） | 中 | 定死：失败后**停派发新任务**（本层未启动 + 后续层 `skipped`），在跑成员**自然结束不 kill**；摘要含 skipped；`clean` 不支持 | dev.2 §3.3、dev.3 用例 |
| 4 | **`-w` 语义模糊**（"项目 + workspace 都构建？"） | 高 | `-w` = **重定向**到 workspace 命令组（`ezmk build -w` ≡ `ezmk workspace build`）；文档 + help 文本明确；不加 `-w` 的成员内构建仍是单项目语义 | dev.2 §3.1、cli.md、help |

## 4 执行步骤（dev / pre 拆分）

### dev 阶段

- [ ] **dev.1 配置与定位**：`ezmk-workspace.toml` 解析 + `locate_workspace_root()` + 成员校验（路径/存在性/嵌套）+ **`[depends] workspace` 解析与环检测 + 被依赖类型校验** + 单测
- [ ] **dev.2 命令与执行**：`Command::Workspace` + cli 解析 + `src/workspace.cpp`（子进程模型 + ThreadPool 并行 + 汇总 + `--member` 依赖闭包 + `--stop-on-error` 精确语义）+ **Kahn 拓扑分层 + 兄弟产物注入（成员自发现，零环境变量）+ 响应文件兜底** + `-w` 重定向 + i18n
- [ ] **dev.3 测试与 CI**：集成测试（拓扑顺序/层内并行/增量隔离/改库 .cpp 消费者只重链/改库 .h 传播/失败汇总/环与类型拒绝）+ CI 步骤 + **全量零回归**
- [ ] **dev.4 i18n 语言变体**（与 workspace 主线并行）：BCP 47 变体标签解析（`zh_CN`/`zh-CN.UTF-8` → `zh-CN`）+ **继承式变体文件**（`locale/zh-TW.json` 只写差异键）+ 回退链（变体 → 基础 → 英文）+ `check_i18n.py` 变体校验 + 单测

### pre 阶段

- [ ] **pre.1 文档与发布收口**：cli.md（workspace 节 + `EZMK_LANG` 变体说明，zh/en 中文基准先行）/ README 命令速览 / 教程（新章或并入 06）/ CHANGES.md 1.3.0 条目 / **non-goals「多项目工作区」条款更新** + 发布门槛复核

### 正式发布

- [ ] **1.3.0**：聚合 dev.1 ~ dev.4 + pre.1；版本置 1.3.0；tag + Release + 三渠道分发（winget/Homebrew/pacman）

---

## 5 兼容性

| 变更 | 影响 | 处理 |
|------|------|------|
| 新增 `ezmk workspace` 命令组 | 纯新增 | 与既有命令不冲突；无别名 |
| 新增 `ezmk-workspace.toml` | 纯新增文件 | 与 `ezmk.toml` 独立解析；定位互不干扰 |
| `[depends] workspace = [...]` 新字段 | 纯新增 | 未声明时零影响；单项目（非成员）声明了但找不到 workspace → 报错提示 |
| 成员内 `ezmk build` 自动解析兄弟产物 | 仅成员生效 | 非成员项目行为不变 |
| 公共 API | 无破坏性变更 | 新命令/新文件/新字段纯增量；单项目路径零改动 |

## 6 跨版本关注点

- **与 2.0.0 解耦**：workspace 纯增量，不依赖任何 deprecation 到期；2.0.0 保持破坏性变更窗口。
- **non-goals 立场更新**：允许**单向非循环成员依赖**这一最小图；完整构建图（环 / 版本 / 平台矩阵 / 可编程图）仍拒绝——见 `docs/non-goals.md`「多项目工作区」条款（pre.1 落地，中文基准先行同步英文）。
- **与包模型互补**：成员依赖 = 开发中源码即改即用；包（目录包/归档包）= 分发与快照隔离。二者并存。
- **并行模型**：层内并行复用线程池；输出加成员前缀防交错；`--stop-on-error` = 失败后停派发（本层未启动 + 后续层 skipped）、在跑成员自然结束不 kill（见 3.9 坑 3）。
- **i18n 语言变体（dev.4）**：BCP 47 标签归一化 + 继承式变体文件（`locale/zh-TW.json` 只写差异键）+ 回退链（变体 → 基础 → 英文）；与 workspace 主线并行，新增 `workspace_*` 键在变体文件未转写时自动回退 `zh`，永不 `{???}`。
- **回归基线**：当前全量 822 用例 / 3922 断言（1.3.0-dev.1 后），新增功能不得引入回归。
- **后续窗口**：shared 运行时依赖 / 成员级过滤（按标签）/ 更多语言变体与完整 BCP 47 归 2.0.0 或后续评估。
