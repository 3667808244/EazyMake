// hello 示例 — 最简可执行项目（教程 01「安装与验证」/ 02「你的第一个项目」）。
//
// 生成：ezmk example hello
// 运行：cd hello && ezmk build && ezmk run
#include <iostream>

int main() {
    std::cout << "Hello from EazyMake example!" << std::endl;
    return 0;
}
